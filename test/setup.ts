// Jest setup file for vulnerability fixer tests

// Set up test environment
process.env.NODE_ENV = 'test';

// Increase timeout for integration tests
jest.setTimeout(30000);

// Export for TypeScript
export {}; 